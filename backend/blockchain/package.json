{
  "name": "@movr/blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy-kyc-registry.ts --network amoy",
    "deploy:token:amoy": "hardhat run scripts/deploy-drive-token.ts --network amoy",
    "deploy:merkle:amoy": "hardhat run scripts/deploy-merkle-distributor.ts --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "dependencies": {
    "ethers": "^6.7.0",
    "dotenv": "^16.3.1"
  }
}
